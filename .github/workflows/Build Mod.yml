name: Build Mod
on: [push, pull_request]

jobs:
  build:
    strategy:
      matrix:
        java: [21]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: 'microsoft'

      # ขั้นตอนนี้จะโหลดไฟล์ gradle-wrapper.jar ของจริงมาให้เองครับ
      - name: Download Valid Gradle Wrapper
        run: |
          mkdir -p gradle/wrapper
          curl -L https://github.com/gradle/gradle/raw/v8.8.0/gradle/wrapper/gradle-wrapper.jar -o gradle/wrapper/gradle-wrapper.jar

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build

      - name: Upload RamReducer-Mod-Fabric
        uses: actions/upload-artifact@v4
        with:
          name: RamReducer-Mod-Fabric-
          path: build/libs/*.jar
